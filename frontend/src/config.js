const config = {
  // Backend dev server: change this to match the port your backend is running on.
  // Backend server default in backend/src/server.js is PORT=process.env.PORT || 5000
  backendUrl: "http://localhost:5000" // Update backend URL/port if your server uses a different port
};

export default config;